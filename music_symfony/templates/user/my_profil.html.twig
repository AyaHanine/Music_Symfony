{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
        <div class="bg-gradient-to-r from-green-400 via-blue-500 to-purple-600 h-32 relative flex items-center justify-center">
            <div class="absolute -bottom-12">
                <div class="w-24 h-24 bg-gray-200 rounded-full border-4 border-white overflow-hidden">
                    <img src="{{ asset('images/avatar.png') }}" alt="Avatar" class="w-full h-full object-cover">
                </div>
            </div>
        </div>

        <div class="pt-16 pb-6 px-6 bg-blue-50">
            <h1 class="text-gray-800 text-2xl font-bold text-center">{{ app.user.firstName }} {{ app.user.lastName }}</h1>
            <p class="text-gray-600 text-center">{{ app.user.email }}</p>
            <div class="mt-4 text-center">
                <span class="inline-block bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm">
                    Rôle: {{ app.user.roles|join(', ') }}
                </span>
            </div>
        </div>

        
    </div>
</div>
{% endblock %}
