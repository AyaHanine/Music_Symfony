{% extends 'base.html.twig' %}

{% block title %}Bienvenue {{ app.user.firstName }}!{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gradient-to-r from-indigo-900 to-teal-800">
        <div class="max-w-4xl mx-auto p-6 bg-gradient-to-r from-indigo-900 to-teal-800 rounded-lg shadow-xl mt-8">
            <h1 class="text-4xl font-semibold text-center text-white mb-6">Bienvenue, {{ app.user.firstName }} {{ app.user.lastName }}! 🎶</h1>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Bouton Liste des Utilisateurs -->
                <form action="{{ path('user_list_page') }}" method="post">
                    <button type="submit" class="w-full bg-indigo-600 text-white py-3 rounded-lg text-xl font-semibold hover:bg-indigo-700 transition duration-300 transform hover:scale-105">
                        Voir la Liste des Utilisateurs
                    </button>
                </form>

                <!-- Bouton Liste des Chansons -->
                <form action="{{ path('app_song_index') }}" method="get">
                    <button type="submit" class="w-full bg-teal-600 text-white py-3 rounded-lg text-xl font-semibold hover:bg-teal-700 transition duration-300 transform hover:scale-105">
                        Voir la Liste des Chansons
                    </button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
